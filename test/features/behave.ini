[behave]
# Scenarios tagged @ory need the Ory Kratos/Hydra/self-enrolment setup; run them with --tags=@ory
default_tags = -@ory

[behave.userdata]
url = http://localhost:80
org_name = MAIN
project_name = test
subject_external_id = test_user
admin_email = admin@example.com
dev_mode = false
secrets_file = ../../etc/secrets.yaml
s3_connection_url = api.s3.localhost
timeout_s = 10
