apiVersion: v1
kind: Secret
metadata:
  name: {{ template "radar-restapi.fullname" . }}-is
  labels:
    app: {{ template "radar-restapi.name" . }}
    chart: {{ template "radar-restapi.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
type: Opaque
data:
  radar-is.yml: {{ .Files.Get "files/radar-is.yml" | b64enc | quote }}
